name: Release: signal

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  signal_release:
    name: Release: signal
    uses: Novolis-Platform/novolis-workflows/.github/workflows/dotnet-auto-release-from-signal.yml@main
    secrets: inherit
