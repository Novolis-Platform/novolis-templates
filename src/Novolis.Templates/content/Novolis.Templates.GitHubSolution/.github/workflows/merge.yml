name: CI: main

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  merge_job:
    name: Preview packages
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: Novolis-Platform/novolis-workflows/.github/workflows/dotnet-publish-preview.yml@main
    secrets: inherit
