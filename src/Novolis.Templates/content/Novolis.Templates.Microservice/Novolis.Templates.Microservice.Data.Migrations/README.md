# Data.Migrations

EF Core migrations for this service. Add migrations from the repository root:

```bash
dotnet ef migrations add Initial --project Novolis.<Service>.Data.Migrations --startup-project Novolis.<Service>.Api
```
